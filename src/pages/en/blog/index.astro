---
import { getCollection } from 'astro:content';
import BlogList from '../../../components/BlogList.astro';
import { en } from '../../../i18n/en';

const posts = await getCollection('blog', ({ data }) => data.locale === 'en' && !data.draft && data.pubDate <= new Date());
---

<BlogList locale="en" strings={en} posts={posts} />
